export enum PermissionCategory {
  BUSINESS = 'business',
  ITEMS = 'items',
  INVENTORY = 'inventory',
  ORDERS = 'orders',
  CUSTOMERS = 'customers',
  PAYMENTS = 'payments',
  EXPENSES = 'expenses',
  REPORTS = 'reports',
  SETTINGS = 'settings',
  USERS = 'users',
}
