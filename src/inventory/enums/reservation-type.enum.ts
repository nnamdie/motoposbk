export enum ReservationType {
  PRE_ORDER = 'PreOrder', // Customer pre-order
  ORDER = 'Order', // Order reservation
  INTERNAL = 'Internal', // Internal reservation
  PROMOTION = 'Promotion', // Reserved for promotion
}
